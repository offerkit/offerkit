#!/usr/bin/env node
// open-voucherify MCP server.
// Phase 8 wires this up against the curated set of oRPC procedures
// (those marked exposeAsTool in their .meta()), exposing both stdio and
// HTTP transports. For now, this is a placeholder so the package builds.
process.stderr.write("ovx-mcp: not yet implemented (Phase 8)\n");
process.exit(0);
