---
"@offerkit/cli": minor
"@offerkit/mcp": minor
---

Expose the full OfferKit API surface through MCP and add a generic CLI `api` command that can invoke any SDK procedure by dotted path with JSON input.
